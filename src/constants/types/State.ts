interface TypeState {
    ROOT: {
        DRAWER: {
            OPEN: boolean
        }
    },
    PAGES: {
        INDEX: {
            ALGORITHMS: Object,
            SELECTED_ALGORITHM: string
        }
    }
}

export default TypeState;